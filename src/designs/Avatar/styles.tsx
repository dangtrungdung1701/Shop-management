import styled from "styled-components";
import tw from "twin.macro";

export const AvatarContainer = styled.div`
  ${tw`w-3.5 h-3.5 rounded-full overflow-hidden`}
`;
export const Img = styled.img`
  ${tw`w-full`}
`;
